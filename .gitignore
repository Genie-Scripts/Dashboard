# テスト用フィクスチャ（実病院データを含むため公開リポにコミットしない）
tests/comedix_sample.html
tests/comedix_sample.png
tests/page.html

# Pythonの仮想環境 (uv)
.venv/
venv/
env/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# データフォルダ（ローカル専用）
data/

# 一時ファイル・作業ログ
Temp/
*.txt
# robots.txt は公開抑制用なので追跡する（*.txt 一括除外の例外）
!robots.txt

# 仕様書・モックアップ（ローカル作業用）
spec/

# Claude Code local settings
.claude/

# 部門別レポートPDF（ローカル印刷・配布専用・再生成物）
dept_reports/

# Comedix週報サマリーPNG／全病院実績まとめPDF（ローカル再生成物・資料室へ手動アップ）
output/comedix/

# 医業収支 推計レポート（ローカル閲覧専用・公開しない）
output/pl_projection.html
output/pl_residuals.csv

# KPIスナップショット（データ由来・自動再生成・公開しない）
output/last_kpi.json

# 粗利推計 recency 補正キャッシュ（データ由来・自動再生成）
output/g_calib_cache.json

# 月末見込み G バックテスト成果物（ローカル分析用・自動再生成）
output/profit_projection_backtest.json
output/profit_projection_backtest_daily.csv

# 外来件数×外来粗利 バックテスト成果物（ローカル分析用・自動再生成）
output/outpatient_profit_backtest.json
output/outpatient_profit_backtest_monthly.csv

# 自己完結HTML（オフライン/Comedix資料室配信用・ローカル再生成物・公開しない）
output/selfcontained/

# 週次ダイジェスト（掲示A4＋メール貼付テキスト・ローカル再生成物・公開しない）
output/weekly_digest/

# 粗利ダッシュボード（院内配布専用・実病院財務データ入り・公開リポに絶対コミットしない）
粗利ダッシュボード_配信用*.html
output/粗利ダッシュボード*

# Cloudflare Pages 配信用ステージング（scripts/build_publish.shの再生成物）
publish/

# wrangler のローカルキャッシュ
.wrangler/

# 生成HTML（Cloudflare Pages へ直接配信するため、生成HTMLはgit管理しない）
# generate_html.py が出力する本体＋旧URL互換リダイレクトスタブ
portal.html
detail.html
dept.html
doctor.html
nurse.html
admission/index.html
inpatient/index.html
operation/index.html
# 孤立レガシー（v2.1移行前の生成物・現行導線から未参照）
admission_index.html
inpatient_index.html
